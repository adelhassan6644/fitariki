import 'package:flutter/material.dart';

class PreviewOfferPage extends StatelessWidget {
  const PreviewOfferPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
